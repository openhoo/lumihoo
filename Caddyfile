{
	admin off
}

:8080 {
	encode gzip zstd

	handle_path /images/* {
		rewrite * /lumihoo-images{path}
		reverse_proxy minio:9000 {
			header_up Host {upstream_hostport}
		}
	}

	handle {
		reverse_proxy app:3000
	}
}
